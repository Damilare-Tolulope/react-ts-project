import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ textAlign: "center", display: "none" }}>
      React - TypeScript Apps
    </header>
  );
};

export default Header;
